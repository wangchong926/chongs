apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: chongs-demo

resources:
  - k8s/namespace.yaml
  - k8s/deployment.yaml
  - k8s/service.yaml

configMapGenerator:
  - name: chongs-web-content
    files:
      - index.html

